[build]
  publish = "."
  functions = "netlify/functions"

# 让前端的 /api/chat 在线上直接命中 Netlify Function
[[redirects]]
  from = "/api/chat"
  to = "/.netlify/functions/chat"
  status = 200
  force = true
